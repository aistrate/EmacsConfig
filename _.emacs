;; Copy this file to the Home folder
;; ("U:\" at work, "C:\Documents and Settings\Adrian" at home, and "~" on the Mac),
;; and rename it to .emacs (use 'mv' on Mac or MSYS, and 'ren' on Windows)

;; Change this to point to the EmacsConfig folder, using forward slashes (/)
(setq load-path (cons "C:/Emacs/EmacsConfig" load-path))

(load "config")
